import { useRouter } from "next/router";

export default function ClientsProjectIdPage() {
    const router = useRouter();

    return (
        <div>
            <h1>This is Clients Project Id Page</h1>
        </div>
    );
}
